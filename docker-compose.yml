# Marco
version: "3"
services:
  vanilla:
    build:
      context: .
      dockerfile: Dockerfile
    image: "iamos/terraria-vanilla:latest"
  tmodloader:
    build:
      context: .
      dockerfile: tModloader.Dockerfile
    image: "iamos/terraria-tmodloader:latest"
